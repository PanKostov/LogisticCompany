/** One minute in milliseconds */
export const ONE_MINUTE_TTL = 60000;
/** Three minute in milliseconds */
export const THREE_MINUTES_TTL = 3 * ONE_MINUTE_TTL;
/** Five minute in milliseconds */
export const FIVE_MINUTES_TTL = 5 * ONE_MINUTE_TTL;
